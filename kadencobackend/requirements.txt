# Core Django + deployment
Django==5.2.8
gunicorn==23.0.0
whitenoise==6.9.0

# Database + env parsing (Neon Postgres)
dj-database-url==2.3.0
psycopg2-binary==2.9.10
python-dotenv==1.1.0

# API + CORS + JWT
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.0
django-cors-headers==4.9.0

# Images/media if needed
pillow==11.1.0

# If your app really needs pandas/numpy in production (e.g. data processing in views), keep them:
# pandas==2.2.3
# numpy==2.2.4
# Otherwise remove or move to requirements-dev.txt